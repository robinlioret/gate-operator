---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: clustergates.gate.sh
spec:
  group: gate.sh
  names:
    kind: ClusterGate
    listKind: ClusterGateList
    plural: clustergates
    singular: clustergate
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.state
      name: State
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterGate is the Schema for the clustergates API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ClusterGate
            properties:
              consolidation:
                description: Defines the consolidation policy of a Gate. By default,
                  at least 1 valid evaluation.
                properties:
                  count:
                    description: Number of consecutive checks to consider the gate
                      opened.
                    type: integer
                  delay:
                    description: Delay between two checks. By default, it's 10s.
                    type: string
                type: object
              operation:
                description: Indicates how to combine the targets results. By default,
                  they will simply be anded.
                properties:
                  invert:
                    description: If true, will invert the result of the Gate. By default,
                      false.
                    type: boolean
                  operator:
                    description: Operation to perform. By default, it is "And".
                    enum:
                    - And
                    - Or
                    type: string
                required:
                - operator
                type: object
              requeueAfter:
                description: Defines the duration between evaluations of a Gate. By
                  default, 60 seconds
                type: string
              targets:
                description: The set of conditions to make the Gate ready.
                items:
                  description: GateTarget defines the conditions for the gate to be
                    available
                  properties:
                    name:
                      description: |-
                        Name of the target. Must be PascalCase. This will be used to make the matching target condition humanly
                        identifiable. Name will be inferred if not specified.
                        // +kubebuilder:validation:Pattern=`^[A-Z][a-zA-Z0-9]*$`
                      type: string
                    selector:
                      description: Selector
                      properties:
                        apiVersion:
                          description: ApiVersion of the resource(s) to target
                          type: string
                        kind:
                          description: Kind of the resource(s) to target
                          type: string
                        labelSelector:
                          description: Select the resources using labels. Incompatible
                            with name selection.
                          properties:
                            matchExpressions:
                              description: matchExpressions is a list of label selector
                                requirements. The requirements are ANDed.
                              items:
                                description: |-
                                  A label selector requirement is a selector that contains values, a key, and an operator that
                                  relates the key and values.
                                properties:
                                  key:
                                    description: key is the label key that the selector
                                      applies to.
                                    type: string
                                  operator:
                                    description: |-
                                      operator represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists and DoesNotExist.
                                    type: string
                                  values:
                                    description: |-
                                      values is an array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. This array is replaced during a strategic
                                      merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: |-
                                matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                map is equivalent to an element of matchExpressions, whose key field is "key", the
                                operator is "In", and the values array contains only "value". The requirements are ANDed.
                              type: object
                          type: object
                          x-kubernetes-map-type: atomic
                        name:
                          description: Name of the resource to target. Incompatible
                            with label selection.
                          type: string
                        namespace:
                          description: Namespace of the resource(s) to target. By
                            default, the namespace of the gate if relevant.
                          type: string
                      required:
                      - apiVersion
                      - kind
                      type: object
                    validators:
                      description: |-
                        Validators defines how the target should be validated. By default, the target will be validated if at least one
                        object was found by the selector regardless of its state.
                      items:
                        description: |-
                          GateTargetValidator defines a part of the logic to evaluate the target.
                          // +kubebuilder:validation:XValidation:rule="(has(self.atLeast) ? 1 : 0) + (has(self.matchCondition) ? 1 : 0) + (has(self.jsonPointer) ? 1 : 0) == 1",message="The validator must have exactly one key."
                        properties:
                          atLeast:
                            description: Validate the target if at least a certain
                              amount of objects is found and matches the other validators
                              if there are ones.
                            properties:
                              count:
                                description: An absolute minimum
                                type: integer
                              percent:
                                description: A percentage of the found objects
                                type: integer
                            type: object
                          jsonPointer:
                            description: JSON pointer to a field
                            properties:
                              pointer:
                                description: Pointer to the desired field
                                type: string
                              value:
                                description: Value to compare to
                                type: string
                            required:
                            - pointer
                            - value
                            type: object
                          matchCondition:
                            description: Desired condition of the resources.
                            properties:
                              status:
                                description: Type of the kubernetes conditions. By
                                  default, "True"
                                type: string
                              type:
                                description: Type of the kubernetes conditions.
                                type: string
                            required:
                            - type
                            type: object
                        type: object
                      type: array
                  required:
                  - selector
                  type: object
                minItems: 1
                type: array
            required:
            - targets
            type: object
          status:
            description: status defines the observed state of ClusterGate
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the Gate resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              consecutiveValidEvaluations:
                description: Current consecutive valid checks
                type: integer
              nextEvaluation:
                description: lastSuccessfulEvaluation defines when was the last time
                  the gate was successfully evaluated.
                format: date-time
                type: string
              state:
                description: Easy access field representing the gate's condition
                type: string
              targetConditions:
                description: List of the targets conditions
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
