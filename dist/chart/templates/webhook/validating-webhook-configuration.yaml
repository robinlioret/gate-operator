apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingWebhookConfiguration
metadata:
    annotations:
        {{- if .Values.certManager.enable }}
        cert-manager.io/inject-ca-from: {{ .Release.Namespace }}/gate-operator-serving-cert
        {{- end }}
    name: gate-operator-validating-webhook-configuration
webhooks:
    - admissionReviewVersions:
        - v1
      clientConfig:
        service:
            name: gate-operator-webhook-service
            namespace: {{ .Release.Namespace }}
            path: /validate-gate-sh-v1alpha1-clustergate
      failurePolicy: Fail
      name: vclustergate-v1alpha1.kb.io
      rules:
        - apiGroups:
            - gate.sh
          apiVersions:
            - v1alpha1
          operations:
            - CREATE
            - UPDATE
          resources:
            - clustergates
      sideEffects: None
    - admissionReviewVersions:
        - v1
      clientConfig:
        service:
            name: gate-operator-webhook-service
            namespace: {{ .Release.Namespace }}
            path: /validate-gate-sh-v1alpha1-gate
      failurePolicy: Fail
      name: vgate-v1alpha1.kb.io
      rules:
        - apiGroups:
            - gate.sh
          apiVersions:
            - v1alpha1
          operations:
            - CREATE
            - UPDATE
          resources:
            - gates
      sideEffects: None
