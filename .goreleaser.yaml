# Based on https://github.com/robinlioret/gitops-promoter/blob/main/.goreleaser.yaml
project_name: gate-operator
version: 2

dist: release_dist

before:
  hooks:
    - go mod tidy
    - go mod download
    - make manifests
    - make generate
    - make build-installer IMG=ghrc.io/robinlioret/gate-operator:{{ .Tag }}

builds:
  - id: gate-operator
    main: ./cmd
    binary: gate-operator
    goarch:
      - amd64
      - arm64
    env:
      - CGO_ENABLED=0
    flags:
      - -v
    goos:
      - linux
      - darwin
    ldflags:
      - -X github.com/robinlioret/gate-operator/common.version={{ .Version }}
      - -X github.com/robinlioret/gate-operator/common.buildDate={{ .Date }}
      - -extldflags="-static"

dockers:
  - image_templates:
      - ghrc.io/robinlioret/gate-operator:{{ .Tag }}-amd64
    dockerfile: Dockerfile
    skip_push: "{{ .IsSnapshot }}"
    use: buildx
    goarch: amd64
    build_flag_templates:
      - "--pull"
      - "--label=org.opencontainers.image.created={{.Date}}"
      - "--label=org.opencontainers.image.title={{.ProjectName}}"
      - "--label=org.opencontainers.image.revision={{.FullCommit}}"
      - "--label=org.opencontainers.image.version={{.Version}}"
      - "--platform=linux/amd64"
      -
  - image_templates:
      - ghrc.io/robinlioret/gate-operator:{{ .Tag }}-arm64
    dockerfile: Dockerfile
    skip_push: "{{ .IsSnapshot }}"
    use: buildx
    goarch: arm64
    build_flag_templates:
      - "--pull"
      - "--label=org.opencontainers.image.created={{.Date}}"
      - "--label=org.opencontainers.image.title={{.ProjectName}}"
      - "--label=org.opencontainers.image.revision={{.FullCommit}}"
      - "--label=org.opencontainers.image.version={{.Version}}"
      - "--platform=linux/arm64"

docker_manifests:
  - name_template: "ghrc.io/robinlioret/gate-operator{{ .Tag }}"
    image_templates:
      - "ghrc.io/robinlioret/gate-operator:{{ .Tag }}-amd64"
      - "ghrc.io/robinlioret/gate-operator:{{ .Tag }}-arm64"

archives:
  - id: binary
    formats:
      - tar.gz
    builds:
      - "gate-operator"
    # this name template makes the OS and Arch compatible with the results of `uname`.
    name_template: >-
      {{ .ProjectName }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}

release:
  prerelease: auto
  draft: true
  extra_files:
    - glob: ./install.yaml

  header: |
    ## gate-operator
    {{ .Date }}

    ### Container images

    - ghrc.io/robinlioret/gate-operator:{{ .Tag }}

  footer: |
    **Full Changelog**: https://github.com/robinlioret/gate-operator/compare/{{ .PreviousTag }}...{{ .Tag }}

changelog:
  use:
    github
  sort: asc
  abbrev: 0
  groups: # Regex use RE2 syntax as defined here: https://github.com/google/re2/wiki/Syntax.
    - title: 'Features'
      regexp: '^.*?feat(\([[:word:]]+\))??!?:.+$'
      order: 100
    - title: 'Bug fixes'
      regexp: '^.*?fix(\([[:word:]]+\))??!?:.+$'
      order: 200
    - title: 'Documentation'
      regexp: '^.*?docs(\([[:word:]]+\))??!?:.+$'
      order: 300
    - title: 'Dependency updates'
      regexp: '^.*?(feat|fix|chore)\(deps?.+\)!?:.+$'
      order: 400
    - title: 'Other work'
      order: 999

  filters:
    exclude:
      - '^test:'
      - '^skip:'
      - '^.*?Bump(\([[:word:]]+\))?.+$'